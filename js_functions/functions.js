// Function to compute the product of p1 and p2
function myFunction(p1, p2) {
  return p1 * p2;
}
myFunction();

// Function to greet someone

function greet() {
  name = prompt("What's your name?");
  greet = document.write(`Hello ${name}!`);
  return greet;
}
greet();
